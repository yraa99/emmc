.program emmc_dat0_rx
.side_set 1 opt

.wrap_target
    pull block      side 1
    out x, 32       side 1

wait_start:
    nop             side 0
    jmp pin wait_start side 1

bitloop:
    nop             side 0
    in pins, 1      side 0
    jmp x-- bitloop side 1
.wrap
